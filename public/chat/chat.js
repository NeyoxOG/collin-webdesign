const $=q=>document.querySelector(q);const params=new URLSearchParams(location.search),id=params.get('id')||'',token=params.get('token')||'',messages=$('#messages'),form=$('#chatForm'),error=$('#chatError'),title=$('#chatTitle'),meta=$('#chatMeta');let lastSignature='';
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const fmt=s=>new Intl.DateTimeFormat('de-DE',{dateStyle:'short',timeStyle:'short'}).format(new Date(s));
function showError(text){error.hidden=!text;error.textContent=text||''}
function render(items){const sig=items.map(x=>x.id).join('|');if(sig===lastSignature)return;lastSignature=sig;messages.innerHTML=items.map(x=>`<div class="msg ${x.sender}"><div class="bubble">${esc(x.body)}</div><small>${x.sender==='admin'?'Collin':x.sender==='system'?'System':'Du'} · ${esc(fmt(x.created_at))}</small></div>`).join('')||'<div class="messages-loading">Noch keine Nachrichten.</div>';messages.scrollTop=messages.scrollHeight}
async function load(){if(!id||!token){showError('Dieser Chat-Link ist unvollständig.');form.hidden=true;return}try{const r=await fetch(`/api/chat?id=${encodeURIComponent(id)}&token=${encodeURIComponent(token)}`,{cache:'no-store'}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Chat konnte nicht geladen werden.');title.textContent=d.inquiry?.project_type||'Dein Projekt';meta.textContent=`Anfrage von ${d.inquiry?.name||'dir'} · ${fmt(d.inquiry?.created_at)}`;showError('');form.hidden=false;render(d.messages||[])}catch(e){showError(e.message);form.hidden=true}}
form.addEventListener('submit',async e=>{e.preventDefault();const textarea=form.elements.message,btn=form.querySelector('button');const text=textarea.value.trim();if(!text)return;btn.disabled=true;try{const r=await fetch('/api/chat',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id,token,message:text})}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Nachricht konnte nicht gesendet werden.');textarea.value='';lastSignature='';await load()}catch(e){showError(e.message)}finally{btn.disabled=false;textarea.focus()}});
$('#copyLink').addEventListener('click',async e=>{try{await navigator.clipboard.writeText(location.href);e.currentTarget.textContent='Link kopiert ✓'}catch{e.currentTarget.textContent='Bitte URL aus der Adresszeile kopieren'}});load();setInterval(()=>{if(!document.hidden)load()},5000);